angular.module('app.settings.indicatorrrenforcer', ['sqplugin'])
.run(function() {
    // TODO: decorate ProfitTargetService and inject template patch.
    // This scaffold exists so the plugin can compile and be incrementally implemented.
    if (window && window.console) {
        window.console.log('[IndicatorRREnforcer] module loaded');
    }
});
